{
  "name": "playwright_framework",
  "version": "1.0.0",
  "description": "Playwright framework built for production level automation testing",
  "main": "index.js",
  "scripts": {
    "build": "npm install && npx playwright install",
    "test": "playwright test --config ./src/config/playwright.config.ts",
    "test:chrome": "playwright test --config ./src/config/playwright.chrome.config.ts",
    "test:firefox": "playwright test --config ./src/config/playwright.firefox.config.ts",
    "test:safari": "playwright test --config ./src/config/playwright.safari.config.ts",
    "create-page": "ts-node src/utils/cli.util.ts create-page-file",
    "create-action": "ts-node src/utils/cli.util.ts create-action-file",
    "create-component": "ts-node src/utils/cli.util.ts create-component-file",
    "create-test": "ts-node src/utils/cli.util.ts create-test-file",
    "generate-report": "allure generate src/reports/results -o src/reports/results/html",
    "show-report": "allure open src/reports/results/html",
    "cleanup-env": "ts-node src/utils/cleanup.util.ts",
    "copy-history": "ts-node src/utils/copyTestHistory.util.ts",
    "move-history": "ts-node src/utils/moveTestHistory.util.ts",
    "send-email": "ts-node src/utils/sendEmail.util.ts",
    "e2e": "npm run copy-history && npm run cleanup-env && (npm run test || true) && npm run send-email && npm run move-history && npm run generate-report && npm run show-report",
    "e2e:chrome": "npm run copy-history && npm run cleanup-env && (npm run test:chrome || true) && npm run send-email && npm run move-history && npm run generate-report && npm run show-report",
    "e2e:firefox": "npm run copy-history && npm run cleanup-env && (npm run test:firefox || true) && npm run send-email && npm run move-history && npm run generate-report && npm run show-report",
    "e2e:safari": "npm run copy-history && npm run cleanup-env && (npm run test:safari || true) && npm run send-email && npm run move-history && npm run generate-report && npm run show-report",
    "lint": "eslint . --ext .ts"
  },
  "keywords": [
    "playwright",
    "typescript",
    "automation",
    "testing",
    "framework"
  ],
  "author": "Md. Hasnat Jamil",
  "license": "ISC",
  "devDependencies": {
    "@faker-js/faker": "^8.4.1",
    "@playwright/test": "^1.52.0",
    "@types/commander": "^2.12.2",
    "@types/fs-extra": "^11.0.4",
    "@types/glob": "^8.1.0",
    "@types/node": "^20.11.7",
    "@types/nodemailer": "^6.4.14",
    "@typescript-eslint/eslint-plugin": "^6.17.0",
    "@typescript-eslint/parser": "^6.17.0",
    "allure-commandline": "^2.26.0",
    "allure-js-commons": "^2.11.1",
    "allure-playwright": "^2.10.0",
    "app-root-path": "^3.1.0",
    "commander": "^11.1.0",
    "dotenv": "^16.4.4",
    "eslint": "^8.56.0",
    "eslint-plugin-playwright": "^0.21.0",
    "fs-extra": "^11.2.0",
    "glob": "^10.3.10",
    "node-global-storage": "^2.0.0",
    "nodemailer": "^6.9.8",
    "ts-node": "^10.9.2",
    "winston": "^3.11.0"
  }
}
